/*
 * Functions specific for the first page
 *
 * @author: Dejan Dežman <dejan.dezman@cosylab.com>
 */

$(document).ready(function(){
	/*loadSystems(true);

	// Wait for dataselect on tags filter
	$('#load_systems').on('dataselected', function(e, data){
		l(data);
		searchForLogs(data.list, true);
	});*/
});